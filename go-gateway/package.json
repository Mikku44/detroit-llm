{
  "name": "detroit-go-gateway-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "4.137.0"
  }
}
